FT-PHPSTORM-CODE-TEMPLATE
=========================

## Configuration
* Open PhpStorm's settings (default: `ctrl + alt + s`);
* Go to `Tools` (last option) -> `Settings Repository`;
* Check `Auto Sync` (recommended);
* In `Read-Only Sources`, click on `+` and add this repository's URL;
* Save settings.
